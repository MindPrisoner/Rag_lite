# RAG 工程化笔记

一个可用的 RAG 系统不只是“检索 + 大模型”。
在工程上，通常还要考虑：

1. 来源引用：让用户知道答案来自哪里
2. 检索结果展示：便于调试和评估召回质量
3. 错误处理：接口超时、空结果、模型失败
4. 服务化：通过 FastAPI 或 Web UI 暴露能力
5. 日志与配置管理：便于后续维护和迭代

如果没有来源引用，即使回答看起来正确，用户也很难信任系统。
因此一个面向展示和求职的 RAG Demo，最好把“答案 + 参考来源”一起输出。
